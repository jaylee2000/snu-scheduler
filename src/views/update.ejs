<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
  </head>
  <body>
	
	  <h1>Update <%= updateSchedule.subjectName %> </h1>
	 
	  <div>
		<form action="/<%= updateSchedule._id %>?_method=PATCH" method="POST">
			<label for="subjectName">Subject Name: </label>
			<input type="text" id="subjectName" name="schedule[subjectName]" value="<%= updateSchedule.subjectName %>"><br>
			
			<% for (let yoil of daysOfWeek) { %>
				<label for="start-<%= yoil[1]%>"><%= yoil[0] %> Start time: </label>
				<input type="number" id="start-<%= yoil[1]%>" name="schedule[start]" value="<%= updateSchedule[yoil[1]].start %>"><br>
				<label for="end-<%= yoil[1]%>"><%= yoil[0] %> End time: </label>
				<input type="number" id="end-<%= yoil[1]%>" name="schedule[end]" value="<%= updateSchedule[yoil[1]].end %>"><br>
			<% } %>
			
			<label for="weight">Importance: </label>
			<input type="number" id="weight" name="schedule[weight]" value="<%= updateSchedule.weight %>">
			
			<button>Submit</button>
		</form>
	  </div>
	  
	 <a href="/">Return to main page</a>
	  
  </body>
</html>
